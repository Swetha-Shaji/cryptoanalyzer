# 🏗️ Project Architecture & Data Flow Diagram

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│              CRYPTOCURRENCY PRICE FORECASTING SYSTEM             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┐
│   CSV DATA   │
│   Bitcoin    │
│  2010-2024   │
└──────┬───────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────┐
│                     DATA LOADING & PREPROCESSING                 │
│                      (data_loader.py)                            │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 1. Load CSV → Parse Dates → Sort Chronologically        │   │
│  │ 2. Handle Missing Values (forward-fill)                 │   │
│  │ 3. Format for Prophet: columns ds (date), y (price)    │   │
│  │ 4. Validate & print summary statistics                  │   │
│  └─────────────────────────────────────────────────────────┘   │
└──────────┬────────────────────────────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────────────────────────────┐
│              EXPLORATORY DATA ANALYSIS (eda.py)                  │
│                                                                  │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────┐  │
│  │ Historical Price │  │ Price Statistics │  │   Volume     │  │
│  │     Chart        │  │   & Distribution │  │    Analysis  │  │
│  │ (saved to plots) │  │  (saved to plots)│  │ (saved plot) │  │
│  └──────────────────┘  └──────────────────┘  └──────────────┘  │
└──────────┬────────────────────────────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────────────────────────────┐
│                  TRAIN/TEST SPLIT                               │
│                (data_loader.py)                                 │
│                                                                 │
│  Total Data: 5110 days                                          │
│  ├─ Training: 5020 days (2010-2024 except last 90 days)       │
│  └─ Testing:  90 days   (last 90 days for evaluation)        │
└──────────┬────────────────────────────────────────────────────────┘
           │
           ├────────────────────────────────┬──────────────────────┐
           │                                │                      │
           ▼                                ▼                      ▼
    ┌──────────────┐            ┌──────────────────┐    ┌──────────────┐
    │  TRAIN DATA  │            │   PROPHET MODEL  │    │   TEST DATA  │
    │  (5020 days) │            │  TRAINING & FIT  │    │  (90 days)   │
    └──────────────┘            └──────────────────┘    └──────────────┘
                                        │
                         ┌──────────────┴──────────────┐
                         │                             │
                         ▼                             ▼
                  ┌─────────────────────┐      ┌─────────────────────┐
                  │ Learns from data:   │      │ Generates forecast  │
                  │ • Trend direction   │      │ • Future dates      │
                  │ • Trend slope       │      │ • Predicted prices  │
                  │ • Seasonality       │      │ • Confidence bands  │
                  │ • Changepoints      │      │ (95% intervals)     │
                  │ • Uncertainty       │      │                     │
                  └─────────────────────┘      └────────┬────────────┘
                                                        │
                                                        ▼
                                              ┌─────────────────────┐
                                              │  FORECAST PLOTS     │
                                              │  (saved as images)  │
                                              │                     │
                                              │ • Historical +      │
                                              │   Future prices    │
                                              │ • Confidence bands │
                                              │ • Uncertainty      │
                                              └─────────────────────┘
                         │
                         ▼
         ┌──────────────────────────────────┐
         │   MODEL EVALUATION & METRICS     │
         │    (model_evaluation.py)          │
         │                                  │
         │  ┌────────────────────────────┐ │
         │  │ On Test Set (90 days):     │ │
         │  │ • MAE (Mean Abs Error)     │ │
         │  │ • RMSE (Root Mean Sq Err) │ │
         │  │ • MAPE (Mean Abs % Error) │ │
         │  │ • Directional Accuracy    │ │
         │  │                            │ │
         │  │ Generate comparison plot: │ │
         │  │ Actual vs Predicted       │ │
         │  └────────────────────────────┘ │
         └──────────────┬───────────────────┘
                        │
         ┌──────────────┴──────────────┐
         │                             │
         ▼                             ▼
    ┌─────────────────┐        ┌──────────────────┐
    │ METRICS PRINTED │        │  PLOTS SAVED TO  │
    │ TO CONSOLE      │        │ static/ FOLDER   │
    │                 │        │                  │
    │ Example:        │        │ • historical.png │
    │ MAE: $1,234     │        │ • forecast.png   │
    │ RMSE: $1,567    │        │ • evaluation.png │
    │ MAPE: 3.45%     │        │ • components.png │
    │ Accuracy: 65%   │        │ • statistics.png │
    └─────────────────┘        └──────────────────┘
         │                             │
         └──────────────┬──────────────┘
                        │
         ┌──────────────┴────────────────────┐
         │                                   │
         ▼                                   ▼
    ┌──────────────────────┐         ┌──────────────────────┐
    │   SAVE TRAINED       │         │  FLASK INITIALIZATION│
    │   MODEL TO PICKLE    │         │  (app.py)            │
    │                      │         │                      │
    │ model/               │         │ • Load all data      │
    │ prophet_model.pkl    │         │ • Start web server   │
    │                      │         │ • Ready for requests │
    └──────────────────────┘         └──────┬───────────────┘
                                            │
                        ┌───────────────────┘
                        │
                        ▼
         ┌──────────────────────────────────────┐
         │     FLASK WEB SERVER STARTS          │
         │   (localhost:5000)                   │
         │                                      │
         │ Routes:                              │
         │ • GET /             → Dashboard      │
         │ • GET /api/forecast → JSON forecast │
         │ • GET /api/metrics  → JSON metrics  │
         │ • GET /about        → About page    │
         └──────────────┬───────────────────────┘
                        │
         ┌──────────────┴──────────────────┐
         │                                 │
         ▼                                 ▼
    ┌───────────────┐            ┌────────────────────┐
    │  BROWSER      │            │  HTML TEMPLATES    │
    │  (localhost   │────────────→│  • index.html      │
    │   :5000)      │            │  • about.html      │
    └───────────────┘            └────────────────────┘
         │                                 │
         │                                 ▼
         │                        ┌────────────────────┐
         │                        │  CSS STYLING &     │
         │                        │  JAVASCRIPT        │
         │                        │  INTERACTIVE       │
         │                        │  FEATURES          │
         │                        └──────────┬─────────┘
         │                                   │
         └───────────────────────────────────┘
                        │
                        ▼
         ┌──────────────────────────────────────┐
         │      USER SEES DASHBOARD             │
         │                                      │
         │  ┌────────────────────────────────┐ │
         │  │ Key Statistics Cards:          │ │
         │  │ • Current Price                │ │
         │  │ • Forecast Price               │ │
         │  │ • Expected Change %            │ │
         │  │ • Model Accuracy               │ │
         │  └────────────────────────────────┘ │
         │                                      │
         │  ┌────────────────────────────────┐ │
         │  │ Forecast Horizon Selector:     │ │
         │  │ [7 Days] [30 Days]             │ │
         │  │ [60 Days] [90 Days]            │ │
         │  └────────────────────────────────┘ │
         │                                      │
         │  ┌────────────────────────────────┐ │
         │  │ Evaluation Metrics:            │ │
         │  │ MAE | RMSE | MAPE | Accuracy  │ │
         │  └────────────────────────────────┘ │
         │                                      │
         │  ┌────────────────────────────────┐ │
         │  │ Visualization Plots:           │ │
         │  │ • Historical Price Chart       │ │
         │  │ • Forecast with Bands         │ │
         │  │ • Actual vs Predicted         │ │
         │  └────────────────────────────────┘ │
         └──────────────────────────────────────┘
                        │
         ┌──────────────┴──────────────────┐
         │                                 │
         ▼                                 ▼
    ┌──────────────┐              ┌──────────────────┐
    │   USER CLICKS│              │ BROWSER SENDS    │
    │  HORIZON BTN │              │  GET /?horizon=30│
    │  (e.g. 30d) │              └────────┬─────────┘
    └──────┬───────┘                       │
           │                               ▼
           └───────────┬───────────────────────────┐
                       │                           │
                       ▼                           ▼
         ┌───────────────────────┐   ┌──────────────────────┐
         │ FLASK RECEIVES        │   │ PROPHET GENERATES    │
         │ REQUEST WITH HORIZON  │   │ NEW FORECAST FOR     │
         │ app.py: index() route │   │ SELECTED HORIZON     │
         └───────────┬───────────┘   │ (7/30/60/90 days)    │
                     │               └────────────┬─────────┘
                     │                            │
                     ▼                            ▼
         ┌───────────────────────────────────────────────┐
         │  FLASK RENDERS index.html WITH:              │
         │  • New forecast price                        │
         │  • New percentage change                     │
         │  • Updated plots                             │
         │  • New prediction bounds                     │
         └───────────────┬───────────────────────────────┘
                         │
                         ▼
         ┌────────────────────────────────┐
         │  BROWSER DISPLAYS UPDATED      │
         │  DASHBOARD WITH NEW FORECAST   │
         │                                │
         │  User can see:                 │
         │  • New predicted price         │
         │  • Updated percentage change   │
         │  • New forecast plot           │
         │  • Prediction range            │
         │  • Confidence bounds           │
         └────────────────────────────────┘
                         │
         ┌───────────────┴────────────────┐
         │                                │
    Loop Back                          Or View
    (User clicks              Different Options
     another horizon)         (About page, etc)
```

---

## Data Flow Diagram

```
┌─────────────┐
│  CSV FILE   │
└──────┬──────┘
       │
       │ load_data()
       ▼
┌──────────────────────┐
│  Raw DataFrame       │ Columns: Date, Open, High, Low, Close, Volume
│  (~5110 rows)        │
└──────┬───────────────┘
       │
       │ preprocess_data()
       ▼
┌──────────────────────┐
│ Prophet Format DF    │ Columns: ds (Date), y (Close Price)
│ (~5110 rows)         │
└──────┬───────────────┘
       │
       │ get_train_test_split()
       ├──────────────────────────────────┬──────────────────────┐
       │                                  │                      │
       ▼                                  ▼                      ▼
   TRAIN_DF                           TEST_DF                 METRICS
   (5020 rows)                         (90 rows)              (computed)
       │                                  │
       │ train_prophet_model()            │ evaluate_model()
       │                                  │
       ▼                                  ▼
   TRAINED MODEL                    MAE, RMSE, MAPE
   (Prophet object)                 Directional Accuracy
       │                                  │
       ├──────────────────────────────────┤
       │                                  │
       │ generate_forecast()              │
       ▼                                  ▼
   FORECAST DF                       EVALUATION PLOT
   (dates + yhat)                    (actual vs predicted)
       │
       └──────────────────┬─────────────────┐
                          │                 │
              plot_forecast()     plot_components()
                          │                 │
                          ▼                 ▼
                    forecast.png      components.png


STATIC FOLDER OUTPUT:
├── historical.png      (EDA visualization)
├── forecast.png        (Forecast with bands)
├── evaluation.png      (Actual vs predicted)
├── statistics.png      (Multi-panel stats)
└── components.png      (Trend & seasonality)

MODEL FOLDER OUTPUT:
└── prophet_model.pkl   (Serialized model)
```

---

## Web Request/Response Cycle

```
USER ACTION                           FLASK PROCESS                    RESPONSE
─────────────────────────────────────────────────────────────────────────────

1. Opens browser to localhost:5000
                     ───GET / ──────────────────→
                                  initialize_app()
                                  └─ Load data
                                  └─ Train model
                                  └─ Generate plots
                                  └─ Calculate metrics
                                  
                                  index(horizon=30)
                                  └─ generate_forecast(30)
                                  └─ get_data_from_cache()
                                  └─ calculate_metrics()
                     ←────────────────────────────
                            Return index.html
                                 (with data)

2. Sees Dashboard with:
   • Current price
   • 30-day forecast
   • Historical plot
   • Forecast plot
   • Evaluation plot


3. Clicks "60 Days" button
                     ───GET /?horizon=60 ──────→
                                  
                                  index(horizon=60)
                                  └─ generate_forecast(60)
                                  └─ Update metrics
                     ←─────────────────────────
                        Return index.html (updated)

4. Sees updated:
   • 60-day forecast price
   • Updated percentage change
   • New forecast plot


5. Clicks "About" link
                     ───GET /about ────────────→
                                  
                                  about()
                                  └─ Render about.html
                     ←─────────────────────────
                           Return about.html

6. Views About page
   • Project documentation
   • Technology stack
   • Model explanation
   • Usage instructions
```

---

## Component Interaction Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                          APP.PY                              │
│  (Flask Application - Central Hub)                           │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ initialize_app()                                       │ │
│  │  ├─ calls data_loader.load_data()                     │ │
│  │  ├─ calls data_loader.preprocess_data()              │ │
│  │  ├─ calls data_loader.get_train_test_split()        │ │
│  │  ├─ calls prophet_model.train_prophet_model()        │ │
│  │  ├─ calls model_evaluation.evaluate_model()          │ │
│  │  ├─ calls eda.plot_historical_price()               │ │
│  │  ├─ calls prophet_model.generate_forecast()         │ │
│  │  ├─ calls prophet_model.plot_forecast()             │ │
│  │  ├─ calls model_evaluation.plot_evaluation()        │ │
│  │  └─ calls prophet_model.save_model()                │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ index()   [GET /]                                     │ │
│  │  ├─ accesses global model                            │ │
│  │  ├─ calls prophet_model.generate_forecast()         │ │
│  │  └─ returns render_template('index.html')           │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ api_forecast()   [GET /api/forecast]                 │ │
│  │  └─ calls prophet_model.generate_forecast()         │ │
│  │  └─ returns JSON with forecast data                 │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ api_metrics()   [GET /api/metrics]                   │ │
│  │  └─ accesses global metrics                          │ │
│  │  └─ returns JSON with metrics data                  │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘

        │                   │                    │
        ▼                   ▼                    ▼

┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐
│  DATA_LOADER.PY  │ │ PROPHET_MODEL.PY │ │ MODEL_EVAL.PY    │
│                  │ │                  │ │                  │
│ load_data()      │ │ train_model()    │ │ evaluate_model() │
│ preprocess()     │ │ forecast()       │ │ print_metrics()  │
│ train_test_split()     plot_forecast()│ │ plot_evaluation()│
└──────────────────┘ │ save_model()     │ │                  │
                    └──────────────────┘ └──────────────────┘
        │                   │                    │
        ▼                   ▼                    ▼
    CSV FILE          PROPHET MODEL          METRICS
                                             DICT
                                                │
                                                ▼
                                            TEMPLATES
                                            (HTML/CSS)
                                                │
                                                ▼
                                            WEB BROWSER
                                            (User Sees)
```

---

## Technology Stack Layers

```
┌─────────────────────────────────────────────────────────┐
│                   PRESENTATION LAYER                     │
│                                                          │
│  Web Browser (HTML/CSS/JavaScript)                      │
│  ├─ index.html (dashboard)                              │
│  └─ about.html (documentation)                          │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│                    ROUTING LAYER                         │
│                                                          │
│  Flask Web Framework                                     │
│  ├─ GET /                → Dashboard                    │
│  ├─ GET /about           → About page                   │
│  ├─ GET /api/forecast    → JSON API                     │
│  └─ GET /api/metrics     → JSON API                     │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│                   BUSINESS LOGIC LAYER                   │
│                                                          │
│  ├─ Data Processing (data_loader.py)                    │
│  │  ├─ Load CSV                                         │
│  │  ├─ Parse & Clean                                    │
│  │  └─ Train/Test Split                                │
│  │                                                       │
│  ├─ Model Training (prophet_model.py)                   │
│  │  ├─ Initialize Prophet                               │
│  │  ├─ Fit Model                                        │
│  │  └─ Generate Forecasts                               │
│  │                                                       │
│  ├─ Model Evaluation (model_evaluation.py)             │
│  │  ├─ Calculate Metrics                                │
│  │  └─ Generate Plots                                   │
│  │                                                       │
│  └─ Data Visualization (eda.py)                         │
│     ├─ Plot Historical                                  │
│     └─ Plot Statistics                                  │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│                   DATA ACCESS LAYER                      │
│                                                          │
│  Python Libraries                                        │
│  ├─ Pandas (data manipulation)                          │
│  ├─ NumPy (numerical computing)                         │
│  ├─ Prophet (time series)                               │
│  ├─ Matplotlib (visualization)                          │
│  ├─ Scikit-learn (metrics)                              │
│  └─ Flask (web framework)                               │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│                   DATA STORAGE LAYER                     │
│                                                          │
│  Files                                                   │
│  ├─ bitcoin_2010-07-17_2024-05-23.csv (input)          │
│  ├─ static/historical.png (output)                      │
│  ├─ static/forecast.png (output)                        │
│  ├─ static/evaluation.png (output)                      │
│  └─ model/prophet_model.pkl (output)                    │
└─────────────────────────────────────────────────────────┘
```

---

## Summary

This architecture diagram shows:

1. **Data Flow**: CSV → Processing → Model → Forecast
2. **System Architecture**: Modular, layered design
3. **Component Interaction**: How modules work together
4. **Technology Stack**: Tools and libraries at each layer
5. **Web Request Cycle**: User interaction with dashboard
6. **Processing Pipeline**: Step-by-step transformation of data

The system is designed to be:
- **Modular**: Each component has a specific responsibility
- **Scalable**: Easy to add features and extend
- **Maintainable**: Clear separation of concerns
- **Efficient**: Optimized data and computation flow
